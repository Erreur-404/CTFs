{
    "configurations": [
        {
            "type": "lua",
            "request": "launch",
            "name": "Debug Current File",
            "program": "${workspaceFolder}/zermatt/zermatt-formatted.lua"
        }
    ]
}